language: java
sudo: false
dist: trusty
jdk:
  - oraclejdk8
  - openjdk8
  - openjdk7
install:
  - mvn test-compile -DskipTests=true -Dmaven.javadoc.skip=true -B -V
script:
  - mvn test cobertura:cobertura -P it-cobertura
after_success:
  - mvn coveralls:report -P it-cobertura
